export const boolToWord = (bool: boolean): string => {
  return bool ? "Yes" : "No";
};

// Solutions
// export const boolToWord = (value: boolean): string => value ? 'Yes' : 'No';
// export const boolToWord = (bool: boolean): string => !!bool ? 'Yes' : 'No';